export component DialogWindow inherits Window {
    width: 200px;
    height: 100px;

    Text {
        text: "I am a dialog";
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}